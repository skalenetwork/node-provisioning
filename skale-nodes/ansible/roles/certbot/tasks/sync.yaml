- name: Ensure base SSL dir
  file:
    state: directory
    path: "{{ base_path }}/ssl/{{ inventory_hostname }}"
    recurse: yes

- name: Copy SSL certs
  copy: src=files/ssl/ dest="{{ base_path }}/ssl"
