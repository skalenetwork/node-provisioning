- name: Mirage migrate
  command:
    cmd: "mirage node migrate {{ base_path }}/init-env --yes"
  register: migrate_cmd

- name: Show migrate command output
  debug:
    msg: "{{ migrate_cmd.stdout }}"